# Questionários

Itens dos instrumentos e suas chaves de codificação.

## Conteúdo esperado

| Arquivo | Conteúdo |
| --- | --- |
| `rei40_itens.csv` | Os 40 itens, fator associado e sentido de pontuação (direto/reverso) |
| `contexto_itens.csv` | Variáveis contextuais e categorias permitidas |
| `chave_codificacao.md` | Como calcular escores por fator |

> O REI-40 deve ser usado na versão validada em português, de forma voluntária.
> Não inclua respostas de participantes aqui — microdados ficam fora do
> repositório (ver `.gitignore` e o protocolo).
